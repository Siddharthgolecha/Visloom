# Implementation — <task title>

<!-- Append-only log. Never delete past entries; strikethrough if reversed.
     Each entry cites the spec.md/plan.md section it implements and carries
     a trust-tier marker:
       - tested-against-real-input
       - tested-with-fixture
       - untested-assumption
       - inherited-from-existing-code -->

## Task tree

<!-- Hierarchical checklist. Tick as items land. -->

- [ ] Step 1 (plan.md §Tactical steps #1)
- [ ] Step 2 (plan.md §Tactical steps #2)

## Deviations

<!-- Cite which spec.md/plan.md section, what changed, why. If it's a
     shortcut, name the ceiling and the upgrade path. Structural
     deviations (changed acceptance criteria, new dependencies,
     contradicted approach) HALT for re-approval — do not silently proceed. -->

## Summary

<!-- Incremental. Derived into the PR description at "ready" time by
     `tools/autodev/task_finish.sh`. Each entry gets a trust-tier marker. -->
