"""Pytest discovery lives here per docs/conventions/testing.md.

Fixtures are intentionally absent — testing.md:36-44 says fixtures
are plain data. The `examples/` directory carries JSON payloads;
each test reads them directly.
"""
